base:
  num_classes: 10
  include_top: False
  image_width: 32
  image_height: 32
  image_channels: 3
  weights: imagenet
  optimizer: Adam
  epochs: 1
  batch_size: 64
  loss_fnc: sparse_categorical_crossentropy

train_model:
  epochs: 1
  batch_size: 8
  steps_per_epoch: 10
